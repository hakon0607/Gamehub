{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "The smallest set of permissions GameHub needs. Note what is absent: no shell plugin, no filesystem plugin, and no HTTP plugin. Every filesystem read and every process start happens in Rust, behind a command that validates its arguments first. The clipboard permissions cover reading what was copied into the local history and writing an item back when the user clicks Copy again; nothing here can send anything anywhere.",
  "windows": [
    "main",
    "quicktools",
    "notify"
  ],
  "permissions": [
    "core:default",
    "core:window:allow-start-dragging",
    "core:window:allow-minimize",
    "core:window:allow-close",
    "core:window:allow-hide",
    "core:window:allow-show",
    "dialog:allow-open",
    {
      "identifier": "opener:allow-reveal-item-in-dir",
      "allow": [
        {
          "path": "**"
        }
      ]
    },
    "updater:default",
    "process:allow-restart",
    "global-shortcut:default",
    "clipboard-manager:allow-read-text",
    "clipboard-manager:allow-write-text"
  ]
}
